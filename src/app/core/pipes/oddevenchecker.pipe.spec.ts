import { OddevencheckerPipe } from './oddevenchecker.pipe';

describe('OddevencheckerPipe', () => {
  it('create an instance', () => {
    const pipe = new OddevencheckerPipe();
    expect(pipe).toBeTruthy();
  });
});
